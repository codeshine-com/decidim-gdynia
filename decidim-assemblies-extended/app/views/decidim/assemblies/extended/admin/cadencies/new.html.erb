<%= decidim_form_for(@form, html: { class: "form new_cadency" }) do |f| %>
  <%= render partial: "form", object: f, locals: { title: t("cadencies.new.title", scope: "decidim.admin") } %>

  <div class="button--double form-general-submit">
    <%= f.submit t("cadencies.new.create", scope: "decidim.admin") %>
  </div>
<% end %>
